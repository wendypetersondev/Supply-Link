# ─────────────────────────────────────────────────────────────────────────────
# Supply-Link Frontend — Environment Variables
# Copy this file to .env.local and fill in the values for your environment.
# NEVER commit .env.local to version control.
# ─────────────────────────────────────────────────────────────────────────────

# Soroban smart contract address on the target network.
# Testnet default is the publicly deployed Supply-Link contract.
# Required: yes
NEXT_PUBLIC_CONTRACT_ID=CBUWSKT2UGOAXK4ZREVDJV5XHSYB42PZ3CERU2ZFUTUMAZLJEHNZIECA

# Target Stellar network. Accepted values: testnet | mainnet
# Controls which RPC URL and network passphrase are used automatically.
# Required: yes
NEXT_PUBLIC_STELLAR_NETWORK=testnet

# Soroban RPC endpoint. Override only if you run a private RPC node.
# Defaults are set in lib/stellar/client.ts based on NEXT_PUBLIC_STELLAR_NETWORK.
# Required: no (auto-derived from NEXT_PUBLIC_STELLAR_NETWORK)
# NEXT_PUBLIC_RPC_URL=https://soroban-testnet.stellar.org

# Stellar network passphrase. Override only if you use a custom network.
# Required: no (auto-derived from NEXT_PUBLIC_STELLAR_NETWORK)
# NEXT_PUBLIC_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# ─────────────────────────────────────────────────────────────────────────────
# Data source
# ─────────────────────────────────────────────────────────────────────────────

# Which repository implementation backs product and event reads and writes.
# Accepted values: mock | contract
#   mock     – seeded in-memory fixtures (lib/mock/*), the default
#   contract – reads and writes through the Soroban contract client
# Any other value falls back to mock. Auditors, attestations and batches always
# resolve to mock: there is no on-chain registry for them yet.
# Required: no (defaults to mock)
# DATA_SOURCE=mock

# Same switch for client bundles, read only when DATA_SOURCE is unset.
# Required: no
# NEXT_PUBLIC_DATA_SOURCE=mock

# ─────────────────────────────────────────────────────────────────────────────
# Invitation links
# ─────────────────────────────────────────────────────────────────────────────

# Public base URL used to build invite links (e.g. https://supply-link.vercel.app).
# Falls back to the request Host header when not set.
# Required: no (auto-derived from request host in dev)
# NEXT_PUBLIC_APP_URL=https://supply-link.vercel.app

# Vercel KV credentials — required in production to persist invitation tokens.
# Leave unset in development; an in-memory store is used automatically.
# Obtain from: Vercel dashboard → Storage → KV → .env.local
# Required: production only
# KV_REST_API_URL=https://<your-kv>.kv.vercel-storage.com
# KV_REST_API_TOKEN=<your-token>

# ─────────────────────────────────────────────────────────────────────────────
# Webhook API authentication
# ─────────────────────────────────────────────────────────────────────────────

# API key for partner-tier webhook subscription management endpoints.
# Required: yes (server-side only — never prefix with NEXT_PUBLIC_)
# In development / E2E tests, set a stable known value here.
# In production, use a randomly-generated secret of at least 32 characters.
PARTNER_API_KEY=change-me-to-a-strong-random-secret

# API key for internal-tier endpoints (higher rate limits).
# Required: no (leave unset unless you need internal-tier access)
# INTERNAL_API_KEY=change-me-to-a-strong-random-secret